# drift port — flow เต็ม · ความเป็นส่วนตัว · ค่าใช้จ่าย · หน้าจอทั้งหมด

> ร่าง 26 ส.ค. 2569 · ต่อจาก `PORT_CONCEPT.html` (25 ส.ค.) ซึ่งวางสถาปัตยกรรมไว้แล้ว
> อันนี้คือชั้นที่ลงมือได้ — เส้นทางผู้ใช้ทุกเส้น จุดขอความยินยอมทุกจุด ตัวเลขทุกตัว
> **หน้าจอดูของจริงที่ `port-studies.html`** (29 หน้า ขนาดมือถือ ฟอนต์และสีเดียวกับแอป)
> ✅ **กอล์ฟตัดสิน 6 ข้อแล้ว 26 ส.ค.** — ดู §11 · ยังไม่ได้เขียนโค้ดอะไรเลย

---

## 1 · หนึ่งหน้าสำหรับตัดสิน

**ปัญหา** — วันนี้จดหมายหนึ่งฉบับ = ลิงก์หนึ่งเส้นที่ต้องยื่นให้กันเอง ผู้รับ **ตอบกลับไม่ได้**
เพราะไม่มีที่อยู่ให้เขียนถึง จดหมายจึงเป็นการพูดฝ่ายเดียวเสมอ ซึ่งขัดกับคำว่าจดหมาย

**เสียเท่าไหร่** — ~500 บาท/ปี (โดเมนอย่างเดียว) ทุกอย่างที่เหลืออยู่ในชั้นฟรี
เพดานก่อนต้องจ่ายจริง: ~4 ล้านฉบับ หรือ ~16,000 คนต่อวัน

**ได้อะไรเมื่อไหร่** — สี่ขั้น ขั้นละใช้ได้จริงในตัวเอง
ขั้น 2 (port แบบยังไม่มีเซิร์ฟเวอร์) พิสูจน์ได้ว่าคนยอมแลกที่อยู่กันไหม **ก่อนจ่ายค่าอะไรเลย**

**ไม่ทำแล้วเกิดอะไร** — แอปยังใช้ได้ แต่ค้างอยู่ที่ "ของเล่นที่ส่งให้กันได้ครั้งเดียว"
และหน้า first-run สร้างไม่ได้ เพราะปลายทางของมันคือหน้าเปิด port (ข้อ 94)

---

## 2 · เก้าเรื่องที่เจอตอนร่าง และต้องแก้ก่อนลงมือ

### 2.1 ⚠️ ชื่อผู้ใช้ห้ามเป็นที่อยู่ port — ข้อ 79 กับข้อ 80 ขัดกันอยู่

ข้อ 80 ตัดสินว่า **เข้าสู่ระบบด้วยชื่อผู้ใช้** · ข้อ 79 ตัดสินว่า **ที่อยู่ต้องเดาไม่ได้ ไม่มีสมุดรายชื่อ**

ถ้าเป็นของชิ้นเดียวกัน (`drift.xx/golf`) ข้อ 79 ตายทันที — ใครก็เดาชื่อคนไทยยอดนิยมแล้วยิงสแปมได้
และแย่กว่านั้นคือ **ชื่อผู้ใช้ที่ใช้ล็อกอินจะกลายเป็นของสาธารณะ** เท่ากับบอกครึ่งหนึ่งของบัญชีให้คนอื่นไปฟรี ๆ

**แก้:** แยกเป็นสองอย่างที่ไม่เกี่ยวกันเลย

| | คืออะไร | ใครเห็น |
|---|---|---|
| ชื่อผู้ใช้ | ใช้ล็อกอินอย่างเดียว | เจ้าของคนเดียว |
| ที่อยู่ port | รหัสสุ่ม 80 บิต | คนที่คุณให้เท่านั้น |
| ชื่อที่แสดง | ปรากฏบนจดหมาย ไม่ต้องเป็นชื่อจริง ซ้ำกันได้ | คนที่มีที่อยู่คุณ |

#### 2.1ก กติกาคำที่ใช้ทั้งโปรเจกต์ — ตัดสิน 26 ส.ค.

**กอล์ฟเลือกทับศัพท์ `drift port`** เพราะตรงกับชื่อแอปและปุ่มที่มีอยู่แล้ว
แต่พอเรียกทับศัพท์ **คำว่า "ที่อยู่" ก็ว่างพอดีให้ใช้กับที่อยู่ port ได้เต็ม ๆ**

| คำ | หมายถึง |
|---|---|
| **drift port** | ตัวระบบ ทับศัพท์เสมอ ไม่แปล |
| **ที่อยู่** | ที่อยู่ port — รหัส 80 บิต |
| **ต้นทาง** | ตำแหน่งบนโลก — *ยืมจากหน้าคลังที่เขียนไว้อยู่แล้วว่า "ที่อยู่ของคุณ (ต้นทาง)"* |

⛔ **ห้ามสลับสองคำหลังเด็ดขาด** — ร่างรอบแรกตั้งชื่อหน้า E2 ว่า "ย้ายที่อยู่"
ซึ่งชนกับ "เอาที่อยู่คืน" ของหน้า C4 ตรง ๆ ทั้งที่เป็นคนละเรื่องกันคนละอย่าง
ตอนนี้ E2 ชื่อ **"ย้ายต้นทาง"**

### 2.2 ⚠️ ที่อยู่ต้องไม่ผูกกับโดเมน

ข้อ 82.4 บอกว่าต้องซื้อโดเมนตั้งแต่ต้น เพราะ *"ที่อยู่ port ทุกอันอยู่ใต้โดเมน"* —
ถูกครึ่งเดียว **ที่อยู่ไม่จำเป็นต้องอยู่ใต้โดเมนเลยถ้าออกแบบให้ตัวรหัสเป็นที่อยู่**

```
ที่อยู่จริง   K7Q2-9MFD-3XBE-R4NW      ← นี่คือที่อยู่
ลิงก์         drift.xx/p/K7Q29MFD3XBER4NW   ← แค่ห่อให้กดได้
```

วันไหนต้องย้ายโดเมน ที่อยู่ทุกอันยังใช้ได้ แค่ห่อใหม่ — **ยังควรซื้อโดเมนอยู่**
เพราะลิงก์คือทางที่คนใช้จริง 99% แต่ความเสี่ยงลดจาก "พังหมด" เหลือ "ต้องแจ้งย้าย"

### 2.3 ⚠️ ไอคอน PWA กลายเป็นของบังคับทันทีที่มี port

`NEXT.md` เขียนไว้ว่า `icons:[]` ว่างจริง และจัดเป็นงานที่ยังไม่เคยออกแบบ
พอมี port มันเลื่อนขั้นเป็น **ของบังคับ** เพราะ iOS ส่ง Web Push ให้เฉพาะ PWA ที่ผู้ใช้
**ติดตั้งลงหน้าจอโฮมแล้ว** เท่านั้น — ไม่มีไอคอน = ไม่มีใครติดตั้ง = คนไอโฟนไม่มีวันรู้ว่าจดหมายมาถึง

และ "รู้ว่าจดหมายกำลังมา" คือหนึ่งในสี่ข้อที่ port ปลดล็อก
⚠️ ต้องยืนยันบนเครื่องจริงตามข้อ 95 — ข้อจำกัดนี้ผมอ่านจากเอกสาร ยังไม่ได้ทดสอบเอง

### 2.4 ✅ กุญแจหายจากกฎ 7 วัน — แก้ได้แล้ว ปิดคำเตือนข้อ 83.1

ข้อ 83.1 เตือนไว้ว่า *"กุญแจคือของชิ้นเดียวที่ดึงกลับจากเซิร์ฟเวอร์ไม่ได้"*
**ไม่จริงในโครงที่ข้อ 79.2 วางไว้** — ก้อนกุญแจที่ห่อด้วยรหัสผ่านถูกเก็บบนเซิร์ฟเวอร์อยู่แล้ว
ซาฟารีลบของในเครื่องทิ้ง = ดึงก้อนนั้นลงมาใหม่แล้วถอดด้วยรหัสผ่าน

**สิ่งที่เหลืออยู่จริงคือความรำคาญ ไม่ใช่การสูญเสีย** — ต้องพิมพ์รหัสผ่านใหม่ทุก 7 วันถ้าไม่ติดตั้ง PWA
(ติดตั้งลงหน้าจอโฮมแล้วกฎ 7 วันไม่บังคับใช้) ซึ่ง**เป็นเหตุผลข้อที่สองที่ไอคอนกลายเป็นของบังคับ**

### 2.5 ⚠️ E2E ทำให้ "รายงานการใช้ในทางที่ผิด" ทำไม่ได้เลย — ยังไม่มีใครออกแบบ

พอมีที่อยู่ ใครก็เขียนถึงเราได้ (PORT_CONCEPT ยอมรับข้อนี้ไว้แล้ว) แต่เอกสารหยุดที่
*"ที่อยู่ต้องเดาไม่ได้"* ซึ่งกัน**สแปมเครื่องจักร**ได้ แต่ไม่กัน**คนที่คุณเคยให้ที่อยู่ไป
แล้วต่อมาเขาคุกคามคุณ** ซึ่งเป็นกรณีที่เกิดจริงมากกว่ามาก

เราอ่านจดหมายไม่ออก จึงตรวจสอบเองไม่ได้ **ต้องออกแบบให้ผู้รับเป็นคนเปิดผนึกให้**

```
ผู้รับกด "รายงาน"
  → แอปถอดรหัสในเครื่อง
  → แสดงให้ผู้รับเห็นชัดว่า "กำลังจะส่งจดหมายฉบับนี้ให้เราอ่าน"
  → ผู้รับกดยืนยัน → ส่งต้นฉบับที่ถอดแล้วมาพร้อมลายเซ็นของผู้ส่ง
```

ลายเซ็นสำคัญ ไม่งั้นใครก็แต่งจดหมายขึ้นมาใส่ร้ายคนอื่นได้
→ **ผู้ส่งต้องเซ็นจดหมายทุกฉบับด้วยกุญแจของตัวเอง** ไม่ใช่แค่เข้ารหัส

**และต้องมีทางที่ไม่ต้องรายงาน:** ปุ่มบล็อก (ของฝั่งเรา ทำงานทันที ไม่ต้องพึ่งเซิร์ฟเวอร์)
กับปุ่ม **เอาที่อยู่คืน** — หมุนที่อยู่ใหม่ ใครที่เคยได้ของเดิมเขียนถึงไม่ได้อีก
คนที่ยังคบกันอยู่ได้ที่อยู่ใหม่อัตโนมัติผ่านจดหมายฉบับถัดไป

### 2.6 ✅ Open-Meteo ไม่ใช่เพดานอีกแล้ว — แต่สัญญาอนุญาตยังเป็น

ข้อ 81.2 เตือนว่า *"สิ่งที่จะกัดจริงคือ Open-Meteo ไม่ใช่ดิสก์"* — **ล้าสมัยไปแล้วตั้งแต่ข้อ 58**
แคชลมทำให้จดหมายหนึ่งฉบับใช้ **0 คำขอ** ราคาต่อวันคงที่ ไม่โตตามจำนวนผู้ใช้

| | ก่อนแคช | หลังแคช (ของจริงวันนี้) |
|---|---|---|
| คำขอต่อจดหมาย | 238 | **0** |
| 1,000 คน | 47,600/วัน ❌ | **5,920/วัน** ✅ |
| 100,000 คน | 4,760,000/วัน ❌ | **5,920/วัน** ✅ |

เพิ่มแผ่นโลกหยาบ 4° เพื่อรองรับเส้นทางนอกไทย → 9,014/วัน ยังอยู่ในโควตาฟรี (ข้อ 58.8)

**และ "ฟ้าที่ port ของอีกฝ่าย" อ่านจากแคชก้อนเดิมได้ ไม่ใช่คำขอใหม่** — พิกัด port หยาบระดับ 11 กม.
ส่วนช่องแคชกว้าง 107 กม. ที่อยู่ port จึงตกในช่องที่ดึงมาแล้วเสมอ **ฟีเจอร์นี้ราคา 0**

⚠️ **เพดานจริงที่เหลือคือสัญญาอนุญาต ไม่ใช่ตัวเลข** — ชั้นฟรีของ Open-Meteo ห้ามใช้เชิงพาณิชย์
วันไหน drift มีรายได้แม้บาทเดียว ต้องย้ายไปแพ็กเกจเสียเงินหรือรันเซิร์ฟเวอร์เอง (ข้อ 58.8)
**ตัวจำกัดของโปรเจกต์นี้เป็นเรื่องกฎหมาย ไม่ใช่เรื่องเทคนิค**

### 2.7 ⚠️ โหมดลิงก์ต้องอยู่ต่อ ห้ามถอด

ถ้าส่งจดหมายได้เฉพาะไปหา port **คนใหม่ที่ยังไม่มีบัญชีจะรับจดหมายไม่ได้เลย**
ซึ่งฆ่าทางเดียวที่แอปนี้เติบโตได้ — คนได้จดหมายก่อน แล้วค่อยอยากมีที่อยู่ของตัวเอง

**สองโหมดอยู่ด้วยกัน:**

| ส่งไปหา | ได้อะไร | ผู้รับต้องมีบัญชีไหม |
|---|---|---|
| port | ตกในกล่องเลย · ตอบกลับได้ · แจ้งเตือนได้ | ต้องมี |
| ลิงก์ (แบบวันนี้) | ยื่นให้กันเอง · ตอบกลับไม่ได้ | ไม่ต้อง |

ลิงก์ที่ส่งไปแล้วทุกเส้นต้องเปิดได้ตลอดไป — **เป็นข้อผูกมัด ไม่ใช่ของแถม**

### 2.8 ⚠️ ที่อยู่เดินทางทางเดียว — ร่างรอบแรกข้ามเรื่องนี้ไปทั้งข้อ

**การรับที่อยู่ของคนอื่นไว้ ไม่ได้แปลว่าเขาเขียนถึงเราได้** มันแปลว่าเราเขียนถึงเขาได้เท่านั้น
เพราะสิทธิ์เขียนถึงใครมาจากการ *ถือที่อยู่ของคนนั้น* ซึ่งเป็นความสัมพันธ์ทางเดียวโดยธรรมชาติ

หน้า C1 รอบแรกมีปุ่มเดียวว่า "เก็บที่อยู่นี้ไว้" ซึ่ง**ทำให้คนเข้าใจว่าเชื่อมกันเรียบร้อยแล้ว**
แล้วไปงงทีหลังว่าทำไมอีกฝ่ายไม่ตอบสักที ทั้งที่เขาเขียนกลับไม่ได้ตั้งแต่แรก

**แก้ด้วยสองปุ่มที่บอกผลต่างกันชัด:**

| ปุ่ม | ได้อะไร |
|---|---|
| **แลกที่อยู่กัน** | เราเขียนถึงเขาได้ · เขาเขียนถึงเราได้ |
| เก็บไว้เฉย ๆ | เราเขียนถึงเขาได้ทางเดียว |

และหน้ารายชื่อ (C2) ต้องมีป้าย **"ทางเดียว"** ไม่งั้นความไม่สมมาตรจะหายไปเงียบ ๆ

### 2.9 ⚠️ ไอคอน PWA เป็นเงื่อนไขของหน้าจอ ไม่ใช่แค่งานตกแต่ง

ต่อจาก §2.3 — พอออกแบบหน้า "เพิ่ม drift ลงหน้าจอโฮม" จริง ๆ ถึงเห็นว่า
**หน้านั้นพูดไม่ได้เลยถ้าไม่มีไอคอน** เพราะมันต้องโชว์ให้เห็นว่ากดแล้วจะได้อะไรบนหน้าจอโฮม
ไอคอนจึงไม่ใช่ของที่ "ค่อยทำทีหลัง" — มันเป็นสิ่งที่ต้องมีก่อนหน้าจอนั้นจะมีอยู่ได้

---

## 3 · flow ทั้งเส้น

### 3.0 หน้าแรกคือลูกโลก — และทางเดินมีแค่สามเส้น

**กอล์ฟชี้ 26 ส.ค. ว่าสองรอบแรกออกแบบห้องครบ แต่ไม่เคยออกแบบทางเดินสักเส้น**
ไม่เคยบอกว่าล็อกอินเสร็จแล้วขึ้นหน้าไหน มีปุ่มอะไรบ้าง จากหน้านี้ไปหน้านั้นยังไง

**สองข้อเดิมบังคับคำตอบไว้อยู่แล้ว ไม่ต้องคิดใหม่:**

| ข้อ | เขียนไว้ว่า | แปลว่า |
|---|---|---|
| **68** | ลูกโลกคือผืนผ้าให้อากาศได้แสดงตัว | **หน้าแรกต้องเป็นลูกโลก ไม่ใช่รายการจดหมาย** — ถ้าทำรายการเป็นหน้าแรก ลูกโลกกลายเป็นของประดับทันที |
| **7** | ห้ามเลือกฉบับให้อัตโนมัติตอนเปิดแอป | **หน้าแรกชี้ได้ แต่เปิดให้ไม่ได้** |

#### 3.0ก โครงหน้าแรก

```
┌──────────────────────────────┐
│ drift            [บัญชี] [เขียน] │  ← สองปุ่ม เท่านั้น
│                              │
│           ลูกโลก              │  ← จดหมายทุกฉบับอยู่บนนี้จริง
│                              │
│      บรรทัดสถานะ 1 บรรทัด      │  ← เปลี่ยนตามสิ่งที่สำคัญที่สุดตอนนั้น
│                              │
│ ▔▔▔ แถบจดหมาย ▔▔▔  ⌃ ดึงขึ้น │  ← peek ของกล่อง · ดึงขึ้น = กล่องเต็ม
└──────────────────────────────┘
```

⛔ **ไม่มีแถบนำทางล่างจอ** — มันจะบังลูกโลกถาวร ซึ่งขัดข้อ 68 ทั้งข้อ

#### 3.0ข ทางเดินสามเส้น

```
หน้าแรก = ลูกโลก
├ [เขียน]  → C2 คนที่เขียนถึงได้ → C3 หน้า port ของเขา → D3 เขียน
│             → ปัดขึ้นปล่อย → A3 การ์ดพยากรณ์ → กลับลูกโลก
├ [บัญชี]  → E1 → E2 ย้ายต้นทาง · E3 อะไรออกจากเครื่อง · E5 ล็อกแอป
│             · E6 เพิ่มลงหน้าจอโฮม · รับจดหมายจากรหัส · E4 ลบบัญชี
└ ดึงแถบล่างขึ้น → D1 กล่องจดหมาย → แตะฉบับ → D5 อ่าน / D2 กำลังมา
```

**ที่เหลือไม่มีปุ่ม เข้าด้วยการแตะสิ่งที่เห็นอยู่แล้ว** — แตะจดหมายบนลูกโลก → อ่าน ·
แตะชื่อผู้ส่ง → C3 · ใน C3 กด `…` → C4 บล็อก/เอาที่อยู่คืน · C5 รายงาน

⚠️ **[เขียน] ผ่าน C3 ก่อนเสมอ ไม่ลัดไป D3 ตรง ๆ** — เพิ่มมาหนึ่งแตะ แต่ได้เห็นฟ้าที่เขาอยู่
และลมระหว่างเราสองก่อนลงมือเขียน ซึ่งเป็นลำดับที่ถูกต้องทางอารมณ์สำหรับแอปนี้

#### 3.0ค เปิดแอปแล้วไปไหน — ตารางที่ยังไม่เคยมี

```
เปิดแอป
├ ยังไม่เคยเปิดเลย         →  A1 หน้าเปิด (first-run)
├ มี port · ล็อกอยู่        →  E5 ล็อกแอป  →  หน้าแรก
├ มี port · ปกติ           →  หน้าแรก
├ เครื่องใหม่ ไม่มีกุญแจ     →  B5 เข้าสู่ระบบ → H4 กำลังคืนกล่อง → หน้าแรก
└ เปิดจากลิงก์ที่อยู่        →  C1 รับที่อยู่
   └ ยังไม่มี port ของตัวเอง →  B1 เปิด port → **กลับมา C1**
```

⚠️ บรรทัดสุดท้ายสำคัญที่สุด — คนที่ได้ลิงก์มาก่อนมี port **ต้องถูกพากลับมาที่เดิม**
ไม่ใช่โยนเข้าหน้าแรกแล้วให้ไปหาลิงก์นั้นเอง ซึ่งส่วนใหญ่หาไม่เจอแล้ว

#### 3.0ง บรรทัดเดียวที่ตัดสินว่าหน้าแรกพูดอะไร

```
ถึงแล้วยังไม่เปิด  >  กำลังมา  >  กำลังไป  >  ยังไม่มีใครรู้จักที่อยู่  >  เงียบ
```

| สถานะ | บรรทัดสถานะ |
|---|---|
| ถึงแล้วยังไม่เปิด | **มีจดหมายถึงคุณ** · จาก มะลิ · แตะเพื่อเปิด |
| กำลังมา | อีก 6 ชั่วโมง · จาก มะลิ · ตอนนี้อยู่เหนืออ่าวไทย |
| กำลังไปอย่างเดียว | ของคุณกำลังไปหา มะลิ · อีก 9 ชั่วโมง |
| **ยังไม่มีใครรู้จักที่อยู่** | **ยังไม่มีใครรู้จักที่อยู่ของคุณ** + ปุ่มแบ่งปันที่อยู่ |
| เงียบ | ตอนนี้ไม่มีจดหมายอยู่กลางลม |

**สถานะที่สี่คือหน้าที่ฆ่าแอป** — คนจะมาอยู่ตรงนี้ทันทีหลังเปิด port เสร็จ
และไม่มีอะไรให้ทำเลย หน้าเปล่าที่ไม่บอกว่าต้องทำอะไรต่อ คือจุดที่คนปิดแล้วไม่กลับมา

#### 3.0จ "คลัง" หายไป กลายเป็น "บัญชี"

ของในคลังวันนี้มีบ้านใหม่ครบทุกชิ้น ไม่มีอะไรตกหล่น

| ของเดิม | ไปอยู่ |
|---|---|
| ชื่อคุณ · Export/Import JSON | E1 บัญชีของฉัน |
| ที่อยู่ของคุณ (ต้นทาง) | E2 ย้ายต้นทาง |
| **รับจดหมายจากรหัส** | E1 — ⛔ **ห้ามตัด** โหมดลิงก์ต้องอยู่ต่อ (§2.7) |
| จำนวนฉบับที่เก็บอยู่ | D1 กล่องจดหมาย |
| ล้างข้อมูลทั้งหมด | E4 ลบบัญชี |
| อะไรออกจากเครื่องนี้บ้าง | E3 |

### 3.1 คนใหม่ ไม่เคยรู้จักแอป (ข้อ 94)

```
A1  เปิดแอป → ลูกโลกหมุนเงียบ ๆ หนึ่งประโยค  "เขียนอะไรก็ได้ แล้วปล่อยให้ลมพาไป"
A2  หน้าเขียน แบบลดรูป — ไม่มีช่อง "ถึงใคร" มีแค่เนื้อความกับปลายทาง
A3  ปัดขึ้น → กระดาษหลุดมือ (แรงต้าน = ลมจริงที่คุณยืนอยู่)
A4  ตัดมาลูกโลก → การ์ดบอก "ลมวันนี้พาไปถึงใน 31 ชั่วโมง"
        ↑ จุดเดียวในแอปที่บอกความจริงข้อใหญ่สุด ว่าจดหมายไม่ถึงทันที
A5  "จดหมายฉบับนี้ยังไม่มีใครรออยู่ปลายทาง — อยาก drift ไปหาใครสักคนไหม"
        → เปิด port   /   ยังก่อน (ใช้แอปต่อได้ ไม่บังคับ)
```

**สามอย่างที่ first-run แก้** — ตรงกับสามข้อที่ข้อ 94.3 บอกว่าคนเขียนครั้งแรกสะดุด

| สะดุดตรงไหน | แก้ตรงไหน |
|---|---|
| ไม่มีใครบอกว่าจดหมายไม่ถึงทันที | A4 บอกก่อนที่จะรอเก้อ |
| ไม่รู้ว่าปลายทางเปลี่ยนเวลาได้ 4 เท่า | A2 โชว์เวลาโดยประมาณทันทีที่เลือกปลายทาง |
| "จาก" ต้องพิมพ์เองทุกครั้ง | A2 ไม่ถามเลย · ถามตอนเปิด port ครั้งเดียว |

### 3.2 เปิด port

```
B1  ชื่อที่แสดง + ที่อยู่ (ปุ่มเดียว "ใช้ตำแหน่งปัจจุบัน" หรือพิมพ์ชื่อเมือง)
B2  ชื่อผู้ใช้ + รหัสผ่าน           ← อีเมลไม่ถามตรงนี้ ถามทีหลังได้
B3  สี่ข้อที่ต้องยอมก่อน            ← เต็มจอ ตัวปกติ ไม่ใช่ตัวเล็กท้ายหน้า
B4  รหัสกู้คืน แสดงครั้งเดียว        ← สร้างในเครื่อง เราไม่เคยเห็น
B5  port พร้อมแล้ว — ที่อยู่ + QR + ปุ่มแบ่งปัน
```

**กุญแจถูกสร้างระหว่าง B2 กับ B3** ในเครื่อง ก่อนที่อะไรจะออกไปหาเซิร์ฟเวอร์

### 3.3 เชื่อมกัน

```
คนที่มี port  →  แบ่งปันที่อยู่ (ลิงก์ / QR / คัดลอก)  →  ครั้งเดียว ไม่ใช่ทุกฉบับ
อีกฝ่ายกดลิงก์ →  C1 "รับที่อยู่นี้ไว้ไหม" → เก็บเข้ารายชื่อ
                    ↳ ยังไม่มี port ของตัวเอง → พาไปเปิด (B1) แล้วกลับมาที่นี่
```

**ไม่มีคำขอเป็นเพื่อน ไม่มีการอนุมัติ** — ให้ที่อยู่ = อนุญาตแล้ว
เพราะที่อยู่เดาไม่ได้ การถือมันอยู่คือหลักฐานว่าคุณให้เอง

### 3.4 หลังจากนั้น

```
D3 เขียน → เลือกจากรายชื่อ port (ไม่ต้องพิมพ์เมืองอีก port รู้ว่าตัวเองอยู่ไหน)
A3 ปัดขึ้นปล่อย (เหมือนเดิมทุกอย่าง)
D2 จดหมายกำลังมา — อีกกี่ชั่วโมง ตอนนี้อยู่เหนืออะไร เจออากาศแบบไหน
D1 ถึง → นอนอยู่ในกล่อง เปิดเมื่อไหร่ก็ได้
D4 ตอบกลับ → กลับไป A3 · ลมขากลับไม่เท่าขาไป
```

---

## 4 · ที่อยู่ port

| | |
|---|---|
| รูปแบบ | Crockford base32 16 ตัว = **80 บิต** |
| แสดงเป็น | `K7Q2-9MFD-3XBE-R4NW` (ขีดคั่นเพื่อให้อ่านออกเสียงได้) |
| ลิงก์ | `https://drift.xx/p/K7Q29MFD3XBER4NW` |
| ตัวอักษรที่ตัดทิ้ง | `I L O U` — Crockford ตัดไว้แล้ว กัน 1/l/I และ 0/O สับสน |

**ทำไม 80 บิต** — 64 บิตยังพอเดาได้ถ้าเซิร์ฟเวอร์ตอบเร็วและมีคนพยายามหลายปี
80 บิตทำให้การไล่เดาแพงกว่าประโยชน์ที่ได้หลายเท่าตัว และยังสั้นพอที่จะพิมพ์มือได้ในกรณีฉุกเฉิน

**เซิร์ฟเวอร์ต้องหน่วงคำขอที่หาที่อยู่ไม่เจอ** ไม่งั้นความเร็วในการตอบกลายเป็นเครื่องมือไล่เดา

---

## 5 · ตัวตน กุญแจ การกู้คืน

### 5.1 กุญแจ

| ชั้น | ใช้อะไร | ทำไม |
|---|---|---|
| คู่กุญแจของ port | ECDH P-256 | มีใน WebCrypto ทุกเบราว์เซอร์ ไม่ต้องพึ่งไลบรารี — ตรงกับกฎ single-file |
| เข้ารหัสจดหมาย | คู่กุญแจชั่วคราวต่อฉบับ + ECDH + HKDF → AES-256-GCM | ผู้ส่งไม่ต้องมีกุญแจของตัวเองก็ส่งได้ |
| ลายเซ็น | ECDSA P-256 | จำเป็นสำหรับ §2.5 — ไม่มีลายเซ็น = แต่งจดหมายใส่ร้ายกันได้ |

⚠️ **ห่อกุญแจส่วนตัวด้วย PBKDF2-SHA256 600,000 รอบ** ซึ่งเป็นสิ่งที่ WebCrypto มีให้
**อ่อนกว่า Argon2id อย่างมีนัยสำคัญ** ต่อการไล่เดาด้วยการ์ดจอ — ยอมรับได้ในขั้นแรกเพราะไม่ต้องพึ่ง wasm
แต่ต้องบันทึกไว้ว่าเป็นหนี้ทางเทคนิค ไม่ใช่ทางเลือกที่ดีที่สุด

### 5.2 ห่อสองชั้น (ข้อ 80.2)

```
กุญแจส่วนตัว ──┬── ห่อด้วยรหัสผ่าน (PBKDF2)      ──→ เก็บบนเซิร์ฟเวอร์
                └── ห่อด้วยรหัสกู้คืน (HKDF ล้วน)  ──→ เก็บบนเซิร์ฟเวอร์
```

รหัสกู้คืนมีเอนโทรปีสูงพออยู่แล้ว (128 บิต) ไม่ต้องยืด · **เราถือทั้งสองก้อนแต่เปิดไม่ออกสักอัน**

### 5.3 ลืมรหัสผ่าน — ตารางที่ต้องแสดงให้เห็นก่อนสมัคร ไม่ใช่หลังลืม

| มีอะไร | ได้อะไรคืน |
|---|---|
| รหัสผ่าน | ทุกอย่าง |
| รหัสกู้คืน | ทุกอย่าง |
| อีเมลอย่างเดียว | ตัวตน · ที่อยู่ port เดิม · จดหมายฉบับใหม่ — **จดหมายเก่าอ่านไม่ได้ตลอดไป** |
| ไม่มีอะไรเลย | เริ่มใหม่หมด ที่อยู่เดิมตายไปพร้อมกัน |

**"อีเมลกู้บัญชี ไม่ได้กู้จดหมาย"** — เขียนตรงตัวแบบนี้ ห้ามใช้คำว่า "กู้คืนบัญชี" ลอย ๆ

---

## 6 · ความเป็นส่วนตัว

### 6.1 เซิร์ฟเวอร์เก็บอะไร

| ช่อง | ทำไมต้องมี |
|---|---|
| ที่อยู่ port | สุ่ม 80 บิต · ไม่มีสมุดรายชื่อ · ค้นไม่ได้ |
| ชื่อที่แสดง | ให้รู้ว่าเขียนถึงใคร — ไม่ต้องเป็นชื่อจริง |
| ตำแหน่งโดยประมาณ | ทศนิยม 1 ตำแหน่ง ≈ 11.12 กม. — ใช้คำนวณลมและเวลา |
| กุญแจสาธารณะ | ผู้ส่งดึงไปเข้ารหัส |
| ก้อนกุญแจส่วนตัวที่ห่อแล้ว × 2 | ให้ใช้ข้ามเครื่องได้ — เราเปิดไม่ออก |
| ชื่อผู้ใช้ + แฮชรหัสผ่าน | เข้าสู่ระบบ |
| อีเมล (ถ้าให้) | กู้บัญชีอย่างเดียว ไม่แสดงต่อใคร |
| คู่ port ที่คุยกัน · เวลาส่ง · เวลาถึง · ขนาด | ต้องรู้ถึงจะส่งมอบตามเวลาได้ |
| ก้อนจดหมายที่เข้ารหัสแล้ว | เก็บไว้ให้ผู้รับมาเปิด |

### 6.2 ไม่เก็บ

ชื่อจริง · ที่อยู่จริง · เบอร์โทร · สมุดรายชื่อในเครื่อง · รหัสอุปกรณ์ ·
พฤติกรรมรายบุคคล · พิกัดละเอียด · **IP เกินกว่าที่ต้องใช้กันการยิงถล่ม (เก็บแบบแฮช ทิ้งใน 24 ชม.)**

### 6.3 แบบจำลองภัย — ใครทำอะไรได้บ้าง

| ผู้โจมตี | ได้ | ไม่ได้ | กันด้วย |
|---|---|---|---|
| เรา / คนที่ยึดเซิร์ฟเวอร์ | ใครคุยกับใคร เมื่อไหร่ ที่ไหนหยาบ ๆ | **เนื้อจดหมาย** | E2E |
| คนไล่เดาที่อยู่ | ไม่ได้อะไร | ทุกอย่าง | 80 บิต + หน่วงคำขอที่ไม่เจอ |
| คนที่เคยได้ที่อยู่คุณไป | เขียนถึงคุณได้ตลอดไป | อ่านของคนอื่น | **บล็อก + เอาที่อยู่คืน** (§2.5) |
| คนที่รู้รหัสผ่านคุณ | ทุกอย่าง | — | ผู้ใช้เป็นคนดูแล |
| คนที่ยืมเครื่องคุณตอนล็อกอินอยู่ | ทุกอย่าง | — | ⏳ ล็อกด้วย PIN — ยังไม่ตัดสิน |
| ผู้ให้บริการเน็ต | ว่าคุณใช้ drift | เนื้อหา · คู่สนทนา | HTTPS |

### 6.4 จุดขอความยินยอม

**ต้องยอมก่อนถึงจะเปิด port ได้** (หน้า B3 · เต็มจอ · ตัวขนาดปกติ)

1. จดหมายถูกเก็บไว้บนเซิร์ฟเวอร์ของเรา ในรูปที่เราเปิดไม่ออก
2. **ลืมรหัสผ่านแล้วเราช่วยกู้จดหมายไม่ได้ เพราะเราไม่มีกุญแจ**
3. ตำแหน่งโดยประมาณจะถูกเปิดเผยต่อคนที่คุณให้ที่อยู่ — เลี่ยงไม่ได้ ระยะทางคือกลไกทั้งหมดของแอป
4. จดหมายที่ส่งไปแล้วจะยังอยู่ในกล่องของผู้รับ แม้คุณลบบัญชี

**ขอแยก ไม่ยอมก็ยังใช้งานได้**

- ตำแหน่งจากเครื่อง → ไม่ให้ก็พิมพ์ชื่อเมืองเองได้
- การแจ้งเตือนเมื่อจดหมายถึง
- อีเมลสำรอง

**ต้องบอกให้รู้ แม้ไม่ต้องกดยอมรับ**

- พิกัดโดยประมาณถูกส่งไปยัง Open-Meteo (บุคคลที่สาม เยอรมนี) — มีแล้วในหน้าคลัง (ข้อ 78)
- ตอนกดลบบัญชี ต้องบอกก่อนยืนยันว่าอะไรหาย อะไรอยู่ต่อ

### 6.5 PDPA — สิ่งที่ต้องมีคำตอบ ไม่ใช่คำตอบเอง

⚠️ ผมไม่ใช่นักกฎหมาย รายการนี้คือ**หัวข้อที่ต้องให้คนที่รู้จริงตรวจ** ไม่ใช่ข้อสรุปทางกฎหมาย

| เรื่อง | สถานะ |
|---|---|
| ฐานทางกฎหมายของแต่ละข้อมูล | ตำแหน่ง = ความยินยอม · การส่งมอบ = สัญญา — **ต้องให้ทนายยืนยัน** |
| สิทธิเข้าถึง / พกพาข้อมูล | ✅ มีทางแล้ว — Export JSON ทำงานในเครื่อง |
| สิทธิขอลบ | ⚠️ **ถูกจำกัดโดยกฎ "จดหมายที่ส่งแล้วเป็นของผู้รับด้วย"** — จุดที่ต้องให้ทนายดูก่อนเปิดใช้ |
| ส่งข้อมูลข้ามประเทศ | Cloudflare + Open-Meteo — ต้องเปิดเผยและหาฐานรองรับ |
| ผู้เยาว์ | PDPA มีเงื่อนไขเรื่องความยินยอมของผู้เยาว์ — **ต้องตัดสินว่าจะกำหนดอายุขั้นต่ำไหม** |
| ผู้ควบคุมข้อมูล | ใครเป็นเจ้าของตามกฎหมาย บุคคลธรรมดาหรือนิติบุคคล — **ยังไม่ตัดสิน** |

**ข้อที่สำคัญที่สุด:** กฎ "จดหมายที่ส่งแล้วเป็นของผู้รับด้วย" ต้องเขียนไว้ที่หน้า B3
**ตอนเปิด port** ไม่ใช่ไปโผล่ในหน้านโยบายที่ไม่มีใครอ่าน

---

## 7 · ค่าใช้จ่าย

### 7.1 เงินจริงต่อปี

| รายการ | ปีแรก | หมายเหตุ |
|---|---|---|
| โดเมน | ~400-600 บาท | กอล์ฟตั้งงบไว้ 300-400 แล้ว (ข้อ 83) |
| Cloudflare Pages (ไฟล์นิ่ง) | **0** | ฟรีไม่จำกัด ไม่นับโควตา Workers |
| Workers + D1 + R2 | **0** | ในชั้นฟรี |
| Open-Meteo | **0** | ชั้นฟรี — **แต่ห้ามเชิงพาณิชย์** |
| ตัวส่งอีเมล | **0 ?** | ⚠️ ยังไม่ตรวจ (ข้อ 82.4) — ต้องการ <100 ฉบับ/เดือน |
| **รวม** | **~500 บาท/ปี** | |

### 7.2 เพดานก่อนต้องจ่ายเพิ่ม

| ตัวจำกัด | ชั้นฟรี | รองรับได้ราว |
|---|---|---|
| R2 เก็บ 10 GB | ← ตัวที่ชนก่อนเพื่อนในทางเทคนิค | **~4 ล้านฉบับ** (2.47 KB/ฉบับ) |
| Workers 100,000 คำขอ/วัน | | **~16,000 คนใช้งานต่อวัน** |
| D1 เขียน 100,000 แถว/วัน | | ไม่ใช่คอขวด |
| Open-Meteo | 10,000 คำขอ/วัน | **ไม่โตตามผู้ใช้แล้ว** — คงที่ 5,920-9,014 |
| **สัญญาอนุญาต Open-Meteo** | **ห้ามเชิงพาณิชย์** | ⛔ **ชนทันทีที่มีรายได้บาทแรก** |

**อ่านได้ว่า: 10,000 คน × 300 ฉบับ = 3 ล้านฉบับ = 7 GB ยังไม่ต้องจ่ายอะไรเลย**
และ**เพดานที่จะชนก่อนเพื่อนจริง ๆ เป็นเรื่องกฎหมาย ไม่ใช่เรื่องเทคนิค**

### 7.3 ทำไม E2E ถึงเป็นสิ่งที่ทำให้อยู่ในชั้นฟรีได้ (ข้อ 82.3)

CPU ชั้นฟรีมีแค่ 10 มิลลิวินาทีต่อคำขอ — น้อยมาก
แต่เพราะการเข้ารหัสและถอดรหัสเกิดในเบราว์เซอร์ทั้งหมด เซิร์ฟเวอร์ไม่เคยถอด ไม่เคยทำดัชนี ไม่เคยค้นหา
มันเป็นแค่ตู้เก็บของที่โง่ที่สุดเท่าที่จะโง่ได้ — **ซึ่งพอดีกับ 10 มิลลิวินาทีอย่างยิ่ง**

**ความเป็นส่วนตัวกับค่าใช้จ่ายเดินไปทางเดียวกันในโปรเจกต์นี้ ไม่ได้แลกกัน**

### 7.4 ถ้าโตเกินชั้นฟรี

Workers Paid ~$5/เดือน ครอบ 10 ล้านคำขอ · R2 คิดตามที่ใช้จริง
⚠️ ตัวเลขนี้ต้องตรวจซ้ำกับหน้าราคาก่อนจ่าย — แต่ลำดับความมากน้อยคือ **หลักร้อยบาทต่อเดือน ไม่ใช่หลักหมื่น**

---

## 8 · หน้าจอทั้งหมด (29 หน้า)

ดูของจริงที่ **`port-studies.html`** — ขนาดมือถือ ฟอนต์ Noto Serif Thai และสีเดียวกับแอป

**รอบสองยุบ 6 หน้า · ตัด 2 หน้าที่ port ไม่ได้แตะ · เพิ่ม 4 หน้าที่จำเป็น**
**รอบสามเพิ่มหมวด H — หน้าแรกและทางเดิน ซึ่งเป็นสิ่งที่ขาดไปทั้งก้อน**

| | หน้า | สถานะ |
|---|---|---|
| **H · หน้าแรก** | H1 หน้าแรก · มีจดหมายถึง | ใหม่ ⭐ |
| | H2 หน้าแรก · กำลังมา | ใหม่ |
| | H3 หน้าแรก · ยังไม่มีใครรู้จักที่อยู่ | ใหม่ ⚠️ หน้าที่ฆ่าแอป |
| | H4 กำลังคืนกล่องจดหมาย | ใหม่ — เคยอยู่ในกอง "ยังไม่ออกแบบ" |
| **A · คนใหม่** | A1 หน้าเปิด | ใหม่ |
| | A2 เขียนฉบับแรก (ไม่มีถึงใคร ไม่มีลงชื่อ) | ลดรูปจากของเดิม |
| | A3 การ์ดพยากรณ์ | ✅ มีแล้ว · +2 บรรทัด |
| | A4 "อยาก drift ไปหาใครสักคนไหม" | ใหม่ |
| **B · บัญชี** | B1 เปิด drift port | 🔀 ยุบ B1+B2 เดิม |
| | B2 ก่อนเปิด — สี่ข้อ + สามอย่างที่ขอ | 🔀 ยุบ B3+E6 เดิม ⚠️ สำคัญสุด |
| | B3 รหัสกู้คืน | ใหม่ |
| | B4 พร้อมแล้ว + QR | ใหม่ |
| | B5 เข้าสู่ระบบ + ลืมรหัสผ่าน | 🔀 ยุบ B6+B7 เดิม |
| **C · เชื่อมกัน** | C1 รับที่อยู่ — แลกกัน / ทางเดียว | ใหม่ ⚠️ แก้ใหญ่ (§2.8) |
| | C2 คนที่เขียนถึงได้ | ใหม่ |
| | C3 หน้า drift port ของอีกฝ่าย | ใหม่ ⭐ |
| | C4 บล็อก / เอาที่อยู่คืน | ใหม่ |
| | C5 รายงานจดหมาย | ใหม่ (§2.5) |
| **D · จดหมาย** | D1 กล่องจดหมาย | ขยายจาก dock เดิม |
| | D2 จดหมายกำลังมา (หน้ารอ) | ใหม่ — `NEXT.md` จดว่ายังไม่เคยออกแบบ |
| | D3 เขียน — เลือกจากรายชื่อ | แก้จากของเดิม |
| | D4 ตอบกลับ | ใหม่ |
| **E · ส่วนตัว** | E1 บัญชีของฉัน | ใหม่ |
| | E2 ย้ายต้นทาง | ใหม่ · เปลี่ยนชื่อจาก "ย้ายที่อยู่" |
| | E3 อะไรออกจากเครื่องนี้บ้าง | ✅ มีแล้ว · ต้องขยาย |
| | E4 ลบบัญชี | ใหม่ |
| | E5 ล็อกแอปด้วยรหัส | ใหม่ (ตัดสินข้อ 3) |
| | E6 เพิ่มลงหน้าจอโฮม | ใหม่ · **จำเป็น** (§2.3, §2.9) |

**ตัดออกจากชุด** — หน้าปัดขึ้นปล่อย และหน้าอ่านจดหมาย ทำไปแล้วและ port ไม่ได้แตะ ·
หน้า "ฉบับทดลองไปไหนต่อ" กลายเป็นแค่แถวหนึ่งใน D1 หลังกอล์ฟตัดสินว่าเก็บไว้

### 8.1 น้ำเสียง — "งดงามนำ ตรงตาม"

ทุกที่ที่ต้องบอกว่าจะเสียอะไร ใช้สองบรรทัดเสมอ

```
บรรทัดบน   ภาษาของแอป      "กุญแจอยู่ในมือคุณคนเดียว"
บรรทัดล่าง  ตรงจนไม่มีทางเข้าใจผิด   "ลืมรหัสผ่านแล้วเราช่วยกู้จดหมายไม่ได้"
```

บรรทัดบนทำให้คนอ่าน บรรทัดล่างทำให้คนเข้าใจ — **ขาดอันไหนก็ล้มเหลวคนละแบบ**

## 9 · กรณีขอบที่ต้องรอด

| สถานการณ์ | ต้องเกิดอะไร |
|---|---|
| มีจดหมายเฟส 1 ในเครื่องอยู่แล้ว แล้วเพิ่งเปิด port | ย้ายเข้ากล่องอัตโนมัติ ไม่ถาม ไม่หาย |
| ผู้รับไม่มี port | ส่งแบบลิงก์ได้เหมือนเดิม (§2.7) |
| ลิงก์เก่าที่ส่งไปแล้ว | เปิดได้ตลอดไป — ข้อผูกมัด |
| ออฟไลน์ตอนกดปล่อย | เขียนได้ · เข้าคิว · ปล่อยจริงตอนเน็ตกลับมา **และเวลาต้องนับจากตอนปล่อยจริง ไม่ใช่ตอนเขียน** |
| เครื่องใหม่ ยังไม่เคยล็อกอิน | ดึงก้อนกุญแจลงมา + รหัสผ่าน → เห็นจดหมายเก่าครบ |
| ซาฟารีลบของในเครื่องทิ้ง (7 วัน) | เหมือนเครื่องใหม่ — ไม่หาย แค่ต้องพิมพ์รหัสผ่าน (§2.4) |
| ย้ายบ้านตอนจดหมายกำลังบิน | ถึงเหมือนเดิม ตามถนนเส้นเก่า (ข้อ 77.1) |
| อีกฝ่ายลบบัญชีตอนจดหมายกำลังบิน | จดหมายในกล่องคุณยังอยู่ · ฉบับที่กำลังไปหาเขาหายไปกับกล่องเขา |
| อีกฝ่ายหมุนที่อยู่หนีเรา | จดหมายฉบับใหม่ส่งไม่ได้ · ฉบับเก่าในกล่องเราไม่กระทบ |
| จดหมายหายกลางทาง (พายุ) | เหมือนเดิม — port ไม่เปลี่ยนกลไกนี้ |
| คนเดียวล็อกอินสองเครื่องพร้อมกัน | ต้องรวมกล่องให้ตรงกัน — **ยังไม่ออกแบบ ⏳** |

---

## 10 · ลำดับการสร้าง

| ขั้น | ทำอะไร | พิสูจน์อะไร | ค่าใช้จ่าย |
|---|---|---|---|
| **1** | ปิดรูที่เหลือ: ไอคอน PWA (§2.3) · ขยายหัวข้อ "อะไรออกจากเครื่องนี้" | — | 0 |
| **2** | **port แบบยังไม่มีเซิร์ฟเวอร์** — ที่อยู่เป็นลิงก์ที่ฝังชื่อ+พิกัดหยาบ แอปจำรายชื่อไว้ในเครื่อง | **คนยอมแลกที่อยู่กันไหม** ก่อนจ่ายค่าอะไร | 0 |
| **3** | first-run (A1-A5) ต่อกับขั้น 2 | คนใหม่เข้าใจแอปไหม | 0 |
| **4** | บัญชี กุญแจ กล่องจริง ตอบกลับ | ของจริง | ~500 บาท/ปี |
| **5** | สิ่งที่ port ปลดล็อก — ฟ้าที่นั่น · ลมระหว่างเรา · รู้ว่าจดหมายกำลังมา | | 0 |

**ขั้น 2 คือของที่ฉลาดที่สุดในตาราง** — ได้ที่อยู่ที่ใช้ได้จริง ได้รายชื่อ ได้การไม่ต้องพิมพ์เมืองอีก
โดยยังเป็นไฟล์ HTML ไฟล์เดียว ไม่มีเซิร์ฟเวอร์ ไม่มีบัญชี ไม่มีค่าใช้จ่าย
ถ้าคนไม่ยอมแลกที่อยู่กันตั้งแต่ขั้นนี้ ขั้น 4 ก็ไม่ต้องทำ

---

## 11 · หกข้อที่ตัดสินแล้ว (26 ส.ค. 2569)

| | คำถาม | ✅ ตัดสินว่า |
|---|---|---|
| 1 | จดหมายฉบับทดลองไปไหนต่อ | **เก็บไว้ก่อน** — อยู่ในกล่องใต้หัวข้อ "ยังไม่ได้จ่าหน้า" ส่งต่อทีหลังได้ |
| 2 | ที่อยู่ยาวแค่ไหน | **80 บิต** — Crockford base32 16 ตัว |
| 3 | ล็อกแอปด้วยรหัส | **มี** — ปิดไว้เป็นค่าเริ่มต้น (หน้า E5) |
| 4 | อายุขั้นต่ำ | ⏳ **ยังไม่ตัดสิน** — กอล์ฟว่า "13+ ก็น่าจะได้มั้ง" ซึ่งเป็นการเดา ไม่ใช่คำตอบ |
| 5 | ปุ่มเอาที่อยู่คืน | **มีตั้งแต่วันแรก** (หน้า C4) |
| 6 | ล็อกอินสองเครื่องพร้อมกัน | **เลื่อนไปหลังขั้น 4** |

#### 11.1 ⚠️ ข้อ 4 ยังเปิดอยู่ และต้องไม่ถูกนับว่าปิดแล้ว

13 ปีเป็นเกณฑ์ของกฎหมายฝั่งอเมริกา (COPPA) **ไม่ใช่ของ PDPA ไทย**
ซึ่งมีเงื่อนไขเรื่องความยินยอมของผู้เยาว์คนละแบบ — ผมไม่มีข้อมูลพอจะยืนยันว่าเลขไหนถูก

**แต่ข้อนี้ไม่บล็อกงาน** เพราะ**ขั้น 2 ไม่มีเซิร์ฟเวอร์ ไม่มีบัญชี จึงไม่ต้องถามอายุเลย**
เลื่อนไปตอบพร้อมทนายตอนขั้น 4 ได้ — ห้ามใส่ช่องอายุลงหน้าจอก่อนถึงตอนนั้น

#### 11.2 "เอาที่อยู่คืน" คืออะไร — บันทึกไว้เพราะเป็นกลไกที่ไม่มีในแอปอื่น

ที่อยู่เป็นรหัสลับที่เจ้าของแจกเอง **การถือรหัสอยู่ = สิทธิ์เขียนถึง**
ไม่มีรายชื่อเพื่อนบนเซิร์ฟเวอร์ให้ถอด เพราะไม่เคยมีการเป็นเพื่อนตั้งแต่แรก
ทางเดียวที่ตัดใครออกจริง ๆ คือ **สุ่มรหัสใหม่ให้ตัวเอง** รหัสเก่าตายทั้งใบ

| | บล็อก | เอาที่อยู่คืน |
|---|---|---|
| กระทบใคร | คนเดียว | **ทุกคนที่มีรหัสเก่า** |
| เขาสมัครใหม่แล้วตามมาต่อ | ยังตามได้ | **ตามไม่ได้** |
| คนที่ยังคบกันอยู่ | ไม่กระทบ | ได้รหัสใหม่อัตโนมัติเมื่อเราเขียนถึงเขาครั้งต่อไป |
| คนที่เราไม่เขียนถึงอีก | ไม่กระทบ | **หายจากกันถาวร** |

## 12 · สิ่งที่ยังไม่ได้ตรวจ ห้ามอ้างเป็นข้อเท็จจริง

- ชั้นฟรีของตัวส่งอีเมล — **ยังไม่เคยเปิดดูสักเจ้า** (ข้อ 82.4 จดไว้แล้ว ยังไม่ได้ทำ)
- iOS Web Push ต้องติดตั้ง PWA ก่อน — อ่านจากเอกสาร **ยังไม่ได้ทดสอบบนเครื่องจริง** (ข้อ 95)
- ราคา Cloudflare ชั้นเสียเงิน — จำมา ต้องเปิดหน้าราคาตรวจก่อนจ่าย
- PDPA ทุกบรรทัดใน §6.5 — **หัวข้อที่ต้องถาม ไม่ใช่คำตอบ**
- ที่อยู่ port 80 บิตเดาไม่ได้จริงแค่ไหน — คำนวณตามทฤษฎี ยังไม่ได้จำลองการโจมตี
- **อายุขั้นต่ำ** — 13 เป็นเกณฑ์อเมริกา ไม่ใช่ไทย ยังไม่มีคำตอบ (§11.1)

### 12.1 ⚠️ เจอตอนตรวจทานหน้าจอ ต้องแก้ในแอปจริง

**หัวจอ `index.html` ยังเขียนว่า "จดหมายสายลม"** ทั้งที่ทั้งโปรเจกต์เรียกตัวเองว่า drift
มาตั้งแต่เปลี่ยนชื่อ และหน้าจอชุดใหม่ทุกหน้าใช้คำว่า drift — ต้องแก้ให้ตรงกันก่อนทำ first-run
