# drift — ผลตรวจงาน 24 ส.ค. 2569

> ตรวจ `index.html` ด้วยการอ่านโค้ด + วัดในเบราว์เซอร์จริงที่ localhost:8777
> ข้อมูลใน localStorage ของกอล์ฟไม่ถูกแตะ (ตรวจก่อน-หลัง: 6 ฉบับ 5,091 ไบต์ เท่าเดิม)
>
> **สถานะ 24 ส.ค. 2569 — กอล์ฟสั่ง "แก้ได้เลย" · แก้แล้ว 7 จาก 9 เรื่อง**
> รายละเอียดสิ่งที่แก้และผลการวัดหลังแก้อยู่ที่ **ข้อ 57** ใน `DRIFT_DECISIONS.md`
> สำรองก่อนแก้: `index.backup-20260824-audit.html`
>
> | ข้อ | สถานะ |
> |---|---|
> | 1 XSS | ✅ แก้แล้ว (esc + ตัวตรวจ schema ที่ทางเข้า) |
> | 2 ริ้วลมหยุดกลางทาง | ✅ แก้แล้ว (`bucket/6`) |
> | 3 จดหมายเก่าเห็นลมเป็นรอยเปื้อน | ✅ แก้แล้ว (อัตราส่วน 2.00 → 0.64) |
> | 4 ปลายทางใต้แถบหัวจอ | ✅ แก้แล้ว (`fitBox` + ZOOM_FIT 0.39) |
> | 5 โควตา API | ⏸ ข้อความ error แก้แล้ว · **ตัวโควตายังต้องตัดสินใจ** |
> | 6 พยากรณ์ 4 วัน | ⏸ บันทึกไว้เป็นข้อจำกัดที่รู้ตัว |
> | 7 ICS ไม่กัน CR | ✅ แก้แล้ว |
> | 8 `fetchWx` ไม่มี timeout | ✅ แก้แล้ว (15 วินาที) |
> | 9 โค้ด §4 | ⏸ เก็บไว้ทำรวดเดียวกับข้อ 44 ตามแผนเดิม |

**สรุป 1 บรรทัด:** เจอ 9 เรื่อง — **1 เรื่องเป็นช่องโหว่ความปลอดภัยที่ยืนยันแล้วว่าใช้โจมตีได้จริง**
และ 2 เรื่องที่ทำให้ "ลมคือพระเอก" เสียหายโดยที่ตัวเลขไม่ฟ้อง

| # | เรื่อง | ระดับ | แก้ยากไหม |
|---|---|---|---|
| 1 | XSS ผ่านลิงก์จดหมาย | **ร้ายแรง** | 1 บรรทัด + ตัวตรวจข้อมูล |
| 2 | ริ้วลมหยุดเปลี่ยนตั้งแต่กลางทาง | **สูง** | 1 ตัวอักษร |
| 3 | จดหมายเก่าเห็นลมเป็นรอยเปื้อน | **สูง** | 1 บรรทัด |
| 4 | ปลายทางไปอยู่ใต้แถบหัวจอ | กลาง | ต้องออกแบบนิดหน่อย |
| 5 | โควตา API ~42 ฉบับ/วัน | **กลาง-สูง (ก่อนเปิดจริง)** | ต้องตัดสินใจเชิงระบบ |
| 6 | พยากรณ์มีแค่ 4 วัน | ต่ำ | รู้ไว้ |
| 7 | ICS ยังไม่กันตัวขึ้นบรรทัด | ต่ำ | 1 บรรทัด |
| 8 | `fetchWx` ไม่มี timeout | ต่ำ | เล็ก |
| 9 | โค้ด §4 ที่ตัดแล้วยังอยู่ | ต่ำ | ลบ |

---

## 1 · XSS ผ่านลิงก์จดหมาย — **ยืนยันแล้วว่ารันโค้ดได้จริง**

**ที่:** `buildLog()` — index.html:2416 และ :2417

```js
if(sev>0) txt='พายุ ลม '+spd+' กม./ชม. — จอดรอ '+fmtDur(hold);
else if(kind===1) txt='ลมพัด '+spd+' กม./ชม.';
...
log.innerHTML=h;
```

`spd` คือ `lg[i][2]` ซึ่ง **มาจากลิงก์ของผู้ส่งทั้งดุ้น** และถูกต่อเข้า `innerHTML` โดยไม่ผ่าน `esc()`
(อีก 5 จุดที่ใช้ `innerHTML` ผ่าน `esc()` หมดแล้ว — จุดนี้จุดเดียวที่หลุด)

**ทดสอบจริงแล้ว:** ใส่ `<img src=x onerror="window.__XSS=1">` ลงในช่อง `spd`
→ แท็กเข้า DOM จริง → `window.__XSS` เปลี่ยนจาก 0 เป็น **1** = โค้ดของผู้ส่งรันแล้ว

**ทำไมถึงร้ายแรงกว่าที่คิด:**
- `ingest()` ตรวจแค่ว่ามี `rec.id` กับ `rec.lg` — **ไม่มีการตรวจชนิดข้อมูลหรือช่วงค่าเลย** (:2431)
- `openRead(L)` ถูกเรียกทันทีใน `ingest()` และ `buildLog()` ถูกเรียก**เสมอ** (:2399)
  → **แค่เปิดลิงก์ก็รันแล้ว ไม่ต้องกดอะไรเพิ่ม**
- payload เป็น base64 บีบอัด → เหยื่อมองไม่ออกว่ามีอะไรอยู่ข้างใน
- สิ่งที่ผู้โจมตีได้: จดหมายทุกฉบับใน localStorage + **ชื่อจริงและพิกัดบ้านของเหยื่อ** (`lw.v1.me`)
  และเขียนทับได้ด้วย

**นี่คือรูปแบบการส่งของเฟส 1 พอดี** — เปิดลิงก์ที่คนอื่นส่งมา ไม่ใช่สถานการณ์สมมติ

**เสนอให้แก้ 2 ชั้น:**
1. `esc(spd)` — ปิดรูทันที
2. **ตัวตรวจ schema ตอน ingest** — ตัวเลขต้องเป็นตัวเลข อยู่ในช่วงที่เป็นไปได้ ความยาว array มีเพดาน
   `bd` ยาวไม่เกิน `MAX_BODY` (ตอนนี้เช็คแค่ตอนเขียน ไม่ได้เช็คตอนรับ)
   นี่คือของที่ต้องมีอยู่แล้วก่อนเฟส 2 ไม่ว่าจะมี XSS หรือไม่

**ตรวจแล้วว่าไม่เป็นปัญหา:** prototype pollution ผ่าน `Object.assign({dir},rec)` — ทดสอบแล้วไม่รั่ว
เพราะ `__proto__` ไปเปลี่ยน prototype ของตัว `L` เอง ไม่ใช่ของ `Object.prototype`

---

## 2 · ริ้วลมหยุดเปลี่ยนตั้งแต่กลางทาง — ผิดกฎที่โค้ดเขียนไว้เอง

**ที่:** `driftStreams()` — index.html:857

```js
const bucket = field ? Math.round(clamp(tf||0,0,1)*6) : 0;   // 0..6
...
const tfb = bucket/12;                                        // 0..0.5  ← ผิด
```

`bucket` วิ่ง 0-6 แต่หารด้วย **12** → `tfb` ไปได้สูงสุดแค่ **0.5**
`wfAt()` แปลงเป็น `u = tf*(NT-1)` = 0..2 → **`u` จึงไม่เคยเกิน 1.0 จาก 2.0**

**แปลว่าช่วงเวลาที่ 3 ของสนามลมไม่เคยถูกวาดเลย** ทั้งที่ดึงมาแล้วและเก็บอยู่ใน URL แล้ว

| | ใช้ tf ได้แค่ไหน | เห็นสนามช่วงไหน |
|---|---|---|
| **เส้นทางของจดหมาย** (`driftRun`) | 0 → 1 | ครบทั้ง 3 ช่วง |
| **ริ้วลมที่ตาเห็น** (`driftStreams`) | 0 → **0.5** | **แค่ 2 ช่วงแรก** |

โค้ดที่ `driftPath()` เขียนกำกับไว้เองว่า:
> *"ริ้วลมที่เห็นกับเส้นทางที่จดหมายเดินต้องมาจากสนามเดียวกัน ไม่งั้นจดหมายจะลอยสวนกระแสที่ตาเห็น"*

**ตอนนี้ครึ่งหลังของทุกการเดินทางละเมิดข้อนี้อยู่** — ลมที่ตาเห็นค้างอยู่ที่กลางทาง
ส่วนจดหมายเดินตามลมของช่วงที่ 3 ที่ไม่มีใครเห็น

น่าจะเป็นเศษที่ค้างจากตอนที่เคยมี 12 ช่วง **แก้: `bucket/6`**

---

## 3 · จดหมายเก่าทุกฉบับเห็นลมเป็นรอยเปื้อนดำ

ข้อ 55 เปลี่ยน `STREAM.STEPS` 22 → 45 (ริ้วยาวเป็นเท่าตัว)
แต่**เส้นทางสำรอง**ที่ใช้ตอนไม่มีสนามเต็ม ยังกระจายเมล็ดในแถบแคบเท่าเดิม (`dist*SPREAD`, SPREAD=0.16)

**วัดจากจดหมายจริงในเครื่อง:**

| | สนามเต็ม (`pk`) | สำรอง (`rg`) |
|---|---|---|
| ริ้วยาว | 1,003 กม. | 392 กม. |
| บริเวณที่โปรยเมล็ด | 1,560 กม. | **195 กม.** |
| **ยาว ÷ บริเวณ** | **0.64** ✅ | **2.00** ❌ |

**ริ้วแต่ละเส้นยาวเป็น 2 เท่าของบริเวณทั้งหมดที่มันเกิด** → ทุกเส้นทับกันเป็นก้อนเดียว
ภาพที่ได้คือรอยแปรงดำ ๆ ก้อนหนึ่ง แล้วครึ่งจอที่เหลือว่างเปล่า

ก่อนข้อ 55 อัตราส่วนนี้คือ 0.98 — **ข้อ 55 ทำให้แย่ลง 2 เท่าโดยไม่มีใครไปดูเส้นทางสำรอง**
เป็นกรณีตรงตัวของกฎ 46.2 ที่เราเขียนเอง

**เรื่องนี้กระทบของจริง: จดหมาย 5 จาก 6 ฉบับในเครื่องกอล์ฟใช้เส้นทางนี้**
(4 ฉบับไม่มี `wf` เลย · `rg` มี `wf` รุ่น 3 ซึ่งถูกปฏิเสธเพราะรุ่นไม่ตรง — พฤติกรรมที่ออกแบบไว้ ถูกแล้ว)

---

## 4 · ปลายทางไปอยู่ใต้แถบหัวจอ — `fitZoom` คิดจากกล้องคนละตำแหน่งกับที่เรียกจริง

คำอธิบายที่เขียนไว้บน `fitZoom` บอกว่า:
> *"ปลายทั้งสองอยู่ห่างจากจุดกลางภาพข้างละครึ่งมุม"* → **สูตรนี้เขียนมาเพื่อกล้องที่อยู่กลางเส้นทาง**

แต่ทุกจุดที่เรียกใช้ (:2183, :2196) วางกล้องไว้ที่ **ตำแหน่งของจดหมาย** (`posAt`)
พอ prog = 0 หรือ 1 ปลายอีกข้างจึงไปอยู่ที่**ขอบจอพอดี ไม่เหลือระยะขอบเลย**

**วัดบนจอ 1280×720 กับจดหมาย ฉ→ช จริง:** ปลายทางตกที่ **y = 3 px**
แต่แถบหัวจอกิน y 0-68 → **ปลายทางอยู่ใต้แถบหัวจอ มองไม่เห็น**

พื้นที่ปลอดภัยจริงคือ y 68-591 = **523 px** แต่ `fitZoom` คิดว่ามี 720 px (มากเกินไป 38%)
เพราะแถบเล่น (591-633) กับแถบจดหมาย (633-721) ทับอยู่ด้วย

**และ drift ยังบวกเพิ่มอีก** — วัดจากของจริง:

| เส้นทาง | ระยะตรง | ลมพาเบนออกข้างสูงสุด |
|---|---|---|
| ฉะเชิงเทรา→เชียงใหม่ | 609 กม. | 47 กม. (7.7%) |
| กรุงเทพ→นราธิวาส | 828 กม. | 162 กม. (19.6%) |
| **ฉะเชิงเทรา→ภูเก็ต** | 709 กม. | **175 กม. (24.6%)** |

**เสนอ:** กรอบภาพควรคิดจาก *กล่องครอบเส้นทางที่ลมพาไปจริง* ไม่ใช่ระยะเส้นตรง
และคิดจาก *พื้นที่ที่ไม่มี UI ทับ* ไม่ใช่ขนาด canvas ดิบ — เข้ากับกฎ 46.2 และกฎ 56.4 ที่เพิ่งเขียน

`revealKm()` ก็ใช้ระยะเส้นตรงเหมือนกัน แต่**อันนั้นไม่ใช่บั๊ก** เพราะมันเป็นรัศมีที่วิ่งตามจดหมาย ไม่ใช่กรอบภาพ

---

## 5 · โควตา API — เพดานราว 42 ฉบับต่อวัน

**ข้อเท็จจริงจากหน้าเงื่อนไขของ Open-Meteo:** 600 ครั้ง/นาที · 5,000/ชม. · 10,000/วัน · 300,000/เดือน

**ที่วัดเองวันนี้:** ยิง 3 ครั้ง (ครั้งละ 238 พิกัด) ห่างกันไม่กี่วินาที → **HTTP 429**
พอรอสักครู่แล้วยิงใหม่ 1 / 10 / 238 พิกัด → ผ่านหมด

3 × 238 = 714 ซึ่งเกิน 600/นาที พอดี → **ตรงกับสมมติฐานว่านับเป็นรายพิกัด ไม่ใช่รายคำขอ**

**[ประเมินโดย AI — เอกสารของ Open-Meteo ไม่ได้ระบุวิธีนับไว้]** อ่านทั้งหน้า terms และหน้า docs แล้วไม่มีข้อความเรื่องนี้
ถ้าจริงตามนี้ **1 ฉบับ = 238 ครั้ง → 10,000 ÷ 238 ≈ 42 ฉบับ/วันทั้งแอป**

ข้อ 55 เพิ่มราคานี้ 2 เท่า (117 → 238 จุด) ซึ่งตอนนั้นเรามองแค่เวลาโหลดกับความยาว URL **ไม่ได้มองโควตา**

**และข้อความ error ยังบอกผิด** — 429 ขึ้นว่า *"ตรวจอินเทอร์เน็ตแล้วลองใหม่"* (:2296)
ทั้งที่อินเทอร์เน็ตปกติดี ควรบอกว่า "สายลมกำลังยุ่ง รอสักครู่แล้วลองใหม่"

**เรื่องนี้ต้องตัดสินใจก่อนเปิดให้คนอื่นใช้** ไม่ใช่เรื่องแก้โค้ดอย่างเดียว

---

## 6-9 · เรื่องเล็ก

**6. พยากรณ์มีแค่ 4 วัน** — `forecast_days=4` (96 ชม.) และ `sampleWx` ตรึงดัชนีไว้ที่ค่าสุดท้าย
การเดินทางที่นานกว่านั้นจะใช้ลมชั่วโมงสุดท้ายค้างไว้
กรณีแย่สุดที่เป็นไปได้: เดินทาง 48 ชม. + จอดรอ 12 ช่วง × 12 ชม. = 144 ชม. · เกิดยาก แต่เกิดได้

**7. `icsLine()` ไม่กันตัวขึ้นบรรทัดแบบ CR** (:2486) — กัน `\` `;` `,` และ LF ครบ แต่ CR ตัวเดียวหลุดได้
ชื่อผู้ส่งที่มี CR แทรกบรรทัด ICS เพิ่มได้ · ผลกระทบจำกัดอยู่แค่ไฟล์ปฏิทิน

**8. `fetchWx` ไม่มี timeout** — คำขอที่ค้างจะค้างตลอดไป ผู้ใช้เห็นแค่ปุ่มที่กดไม่ได้

**9. โค้ด §4 ที่ข้อ 18 สั่งตัดยังอยู่ครบ** — `DETOUR_FRAC/MIN/MAX`, `LOSS_DETOUR`, `mode==='d'`,
`p.simD`, หน้าเลือกข้าม/อ้อม (:2314-2328) · ต้องลบก่อนสร้างข้อ 44

---

## ตรวจแล้วว่าไม่มีปัญหา

- `esc()` ครอบ `innerHTML` อีก 5 จุดครบ (ชื่อคน ชื่อเมือง เนื้อจดหมาย)
- prototype pollution ผ่าน payload — ทดสอบแล้วไม่รั่ว
- localStorage ใช้ไม่ได้ → ถอยไปเก็บในหน่วยความจำ ไม่พังทั้งใบ
- `_driftCache` / `_streamCache` / `_wfCache` ถูกล้างทุกครั้งที่ `saveAll()` ไม่โตไม่หยุด
- เส้นทางของผู้ส่งกับผู้รับตรงกัน (ตรวจในรอบข้อ 55: ต่างกัน 0.000000 กม.)
- ปฏิเสธ `wf` รุ่นเก่าแล้วถอยไปสนาม 12 ค่า — ทำงานถูกตามที่ออกแบบ
- ไม่มี error ใน console ตลอดการตรวจ

---

## ลำดับที่เสนอ

1. **ข้อ 1 (XSS)** — ปิดรูก่อน แล้วค่อยทำตัวตรวจ schema ให้ครบ
2. **ข้อ 2 (`bucket/6`)** — ตัวอักษรเดียว ได้ลมกลับมาครึ่งหนึ่งที่หายไป
3. **ข้อ 3 (เส้นทางสำรอง)** — จดหมายเก่าของกอล์ฟเองยังหน้าตาไม่ดีอยู่
4. **ข้อ 5 (โควตา)** — ต้องคุยกัน ไม่ใช่แค่แก้โค้ด
5. **ข้อ 4 (กรอบภาพ)** — ทำพร้อมกับซูมรอบหน้า
6. ที่เหลือเก็บกวาดรวดเดียวตอนตัด §4 (ข้อ 9)
