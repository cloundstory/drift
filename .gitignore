# สำเนาสำรองระหว่างพัฒนา — ตั้งแต่มี git แล้ว ประวัติอยู่ใน git แทน
# ไฟล์ยังอยู่ในเครื่องเหมือนเดิม แค่ไม่ถูกส่งขึ้น repo
# (ถ้าขึ้น repo แล้วเปิด GitHub Pages ไฟล์พวกนี้จะถูกเสิร์ฟออกไปด้วย ซึ่งไม่ต้องการ)
index.backup-*.html

# ซ้ำกับโฟลเดอร์ proto/ ทั้งดุ้น
files.zip

# ตั้งค่าเครื่องมือเฉพาะเครื่องนี้ — มี path เต็มของ python บนวินโดวส์อยู่ข้างใน
# เครื่องอื่นเอาไปใช้ไม่ได้
.claude/

# ของระบบปฏิบัติการ / เครื่องมือ
Thumbs.db
desktop.ini
.DS_Store
__pycache__/
*.pyc
.vscode/
.idea/

# หมายเหตุ: โฟลเดอร์ wind/ **ต้องอยู่ใน repo**
# เพราะ .github/workflows/wind.yml เขียนไฟล์แคชลมกลับเข้ามาทุก 6 ชั่วโมง
# และแอปอ่านจากไฟล์นั้นตรง ๆ (ข้อ 58)

# ภาพเทียบที่สร้างใหม่ได้จาก contrast-studies.html เสมอ
contrast-compare.png
contrast-after.png
