# -*- coding: utf-8 -*-
"""ลูกโลกไม่มีเนื้อของตัวเอง — วัดแล้วทั้งใบทึบแค่ 4-12% (ข้อ 129)
   เพิ่มชั้นพื้นทรงกลมที่ทึบจริง วาดใต้ทุกชั้น
   ⚠️ baseA ตั้งต้น 0 = ไม่เปลี่ยนอะไรจากเดิมแม้แต่พิกเซลเดียว"""
import io, sys

APP = 'index.html'
s = io.open(APP, encoding='utf-8').read()
n = 0


def sub(old, new):
    global s, n
    if old not in s:
        sys.exit('NOT FOUND: ' + old[:90])
    s = s.replace(old, new, 1)
    n += 1


sub("""  dayA:  1,    /* เนื้อทรงกลมฝั่งกลางวัน */
  nightA: 1,   /* ชั้นมืดฝั่งกลางคืน */
};""",
    """  dayA:  1,    /* เนื้อทรงกลมฝั่งกลางวัน */
  nightA: 1,   /* ชั้นมืดฝั่งกลางคืน */

  /* ---- พื้นทรงกลม (ข้อ 129) ----
     วัดที่ pixel จริง: ลูกโลกทั้งใบทึบแค่ 4-12% — กลาง .125 ขอบ .039
     แปลว่า 88-96% ของสีที่ตาเห็นคือ *พื้นแอป* ที่มองทะลุผ่านฟิล์มบาง
     โหมดสว่างรอดเพราะพื้นสว่างอยู่แล้ว · พอพื้นมืด ลูกโลกก็มืดตามทันที
     📌 ทางแก้ไม่ใช่ดันความทึบของฟิล์มทีละนิด (ไล่ระดับจะเพี้ยน)
        แต่ให้ลูกโลกมี *เนื้อ* ของตัวเองรองอยู่ข้างใต้
     ⓘ baseA 0 = ไม่มีชั้นนี้ = พฤติกรรมเดิมเป๊ะ */
  base:  '236,238,241',
  baseA: 0,
};""")

sub("""    /* 2. เนื้อทรงกลมฝั่งกลางวัน — จุดสว่างอยู่""",
    """    /* 1ก. พื้นทรงกลม — เนื้อของโลกเอง ไม่ใช่พื้นแอปที่มองทะลุ (ข้อ 129)
       ต้องวาดก่อนทุกชั้น เพราะทุกชั้นที่เหลือเป็นฟิล์มโปร่งที่วางทับมัน */
    if(SKIN.baseA>0){
      c.fillStyle='rgba('+SKIN.base+','+Math.min(1,SKIN.baseA).toFixed(3)+')';
      c.beginPath(); c.arc(CX,CY,R,0,Math.PI*2); c.fill();
    }
    /* 2. เนื้อทรงกลมฝั่งกลางวัน — จุดสว่างอยู่""")

io.open(APP, 'w', encoding='utf-8').write(s)
print('patched', n, 'sections')
