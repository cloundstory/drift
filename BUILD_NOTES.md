# จดหมายสายลม — BUILD NOTES (เฟส 1)

> บันทึกสิ่งที่ build จริง + ค่าที่ตั้งไว้ ณ วันที่ 22 ส.ค. 2569
> เอกสารต้นทาง: `LETTER_WIND_CONCEPT.md` (กลไก) + `proto/LETTER_WIND_ART.md` (งานภาพ)

---

## ไฟล์

| ไฟล์ | คืออะไร |
|------|---------|
| `index.html` | **ตัวแอปทั้งหมด** — single-file, ไม่มี dependency, ไม่มี build step |
| `sw.js` | service worker (ทำงานเฉพาะตอน host บน http/https — เปิดจากไฟล์ในเครื่องก็ยังใช้ได้ครบ แค่ไม่มี offline) |
| `.claude/launch.json` | ตั้ง local server ไว้ทดสอบ (`python -m http.server 8777`) |
| `proto/` | prototype 4 ไฟล์ + ART SPEC (แตกจาก `files.zip`) — เก็บไว้อ้างอิงตอนจูนภาพ |

เปิดใช้: ดับเบิลคลิก `index.html` ได้เลย (Chrome/Edge/Safari)
ทดสอบแบบมี URL จริง:

```bash
python -m http.server 8777
```

---

## ทำอะไรได้แล้ว (ครบ core loop เฟส 1)

1. **ตั้งตัวตน** — ชื่อ + ต้นทาง (ค้นชื่อเมืองผ่าน Open-Meteo Geocoding หรือกดใช้ GPS)
2. **เขียนจดหมาย** — ผู้รับ + ปลายทาง + ข้อความ (เพดาน 1,100 ตัวอักษร)
3. **ถามสายลม** — ดึงพยากรณ์จริง 13 จุดบนเส้น great-circle ในคำขอเดียว → คำนวณเวลาถึง + จุดที่จะเจอพายุ
4. **ทางเลือกเส้นทาง** — โผล่เฉพาะเมื่อเจอพายุรุนแรง (0–1 ครั้งต่อฉบับ ตามกฎเหล็ก §4)
5. **ส่ง = ลิงก์** — ข้อมูลทั้งฉบับถูกบีบอัด (deflate) ฝังใน URL hash → ส่งทาง LINE/แชท
6. **รับ** — เปิดลิงก์ → ถอด → เก็บลง localStorage → ขึ้น globe อัตโนมัติ
7. **globe** — ปัดหมุน + inertia, เส้นทาง great-circle มี occlusion, จดหมาย = จุดทองเคลื่อนตาม timestamp, พายุเป็น spiral glyph
8. **อ่านจดหมาย** — อ่านได้ต่อเมื่อ **ถึงแล้ว** เท่านั้น (ยังไม่ถึง = เห็นสถานะ/ETA); กระดาษมีร่องรอยตามที่เจอจริง
9. **บันทึกการเดินทาง** — log ซ่อนไว้ กดดูได้ (ของแถมแบบ B)
10. **จดหมายหาย** — roll ครั้งเดียวตอนส่ง + เตือนก่อนส่ง + ผู้เขียนได้ closure ("สายลมพัดหลงทาง") + ข้อความยังอยู่กับผู้เขียน
11. **Export / Import JSON** — มีตั้งแต่แรกตามข้อกำหนด; ปุ่มล้างข้อมูล **บังคับ export ให้ก่อนเสมอ**
12. **PWA** — manifest ฝังในไฟล์ + service worker (network-first สำหรับตัวหน้า, ไม่แคช API อากาศ) → ติดตั้งลงจอโฮมได้, เปิดซ้ำได้ตอนออฟไลน์
13. **วางลิงก์ในแท็บที่เปิดอยู่แล้วก็รับได้** (`hashchange`) ไม่ใช่แค่ตอนเปิดใหม่
14. **ทางส่งสำรอง 3 ทาง** — ลิงก์ / ไฟล์จดหมาย (.json) / วางรหัสเปล่า (คลัง → รับจดหมายจากรหัส)
15. **เตือนตอนถึง** — ไฟล์ปฏิทิน .ics (ปฏิทินในเครื่องเตือนเอง ไม่ต้องเปิดแอปค้าง) + Notification ของเบราว์เซอร์ + เด้ง toast ตอนเปิดแอปแล้วพบว่ามีจดหมายถึงระหว่างที่ไม่อยู่

---

## ค่าที่ตั้งไว้ — ตอบ open questions §11 (ทั้งหมดแก้ที่ `CFG` จุดเดียว)

| ค่า | ตั้งเป็น | ที่มา / เหตุผล |
|-----|---------|----------------|
| `SPEED` | 416.7 กม./ชม. | 20,000 กม. = 48 ชม. ตามสเกลที่ตกลง (กรุงเทพ→โตเกียว ≈ 11 ชม., →ซานฟรานซิสโก ≈ 31 ชม.) |
| `FLOOR_H` | **6 ชม.** | ระยะ 15 กม. ก็ยังใช้ 6 ชม. — ทดสอบแล้ว |
| `WIND_CAP` / `WIND_REF` | **±28%** / หาร 150 | ลมตรงทิศ ~42 กม./ชม. = เร็วขึ้นเต็มเพดาน; ไม่ให้กลายเป็นฟิสิกส์น่าเบื่อ |
| `HOLD_KMH` | **50 กม./ชม.** | เกินนี้ = พายุปานกลาง → **จอดรอ** (≈ Beaufort 7) |
| `LOST_KMH` | **88 กม./ชม.** | เกินนี้ = พายุรุนแรง → เสี่ยงหาย (≈ Beaufort 10 พายุจริง ๆ นาน ๆ เจอที) |
| เวลาจอด | ปานกลาง 0.5–6 ชม. · รุนแรง 2–12 ชม. | ผันตามความแรงลมจริง |
| `DETOUR` | +35% ของเวลาเดิม (ต่ำสุด 4 ชม. สูงสุด 14 ชม.) | "อ้อม" ต้องช้าลงรู้สึกได้ แต่ไม่ถึงขั้นไม่มีใครเลือก |
| โอกาสหาย | **ข้าม 14% · อ้อม 2%** | เกิดได้เฉพาะเส้นที่มีพายุรุนแรงจริงเท่านั้น |
| `SEGS` | 12 ช่วง | ละเอียดพอให้เจอพายุ แต่ยิง API ครั้งเดียว |
| `MAX_BODY` | 1,100 ตัวอักษร | วัดจริง: ไทย 840 ตัวอักษร → payload 417 ตัวอักษร (ปลอดภัยใต้เพดาน URL 2,000) |
| ร่องรอย | ฝน = ฝนสะสม/40 มม. · ยับ = จำนวนช่วงพายุ (สูงสุด 3) · อายุ = วันเดินทาง (อิ่มตัวที่ 6 วัน) | เพดานตั้งให้ "ผ่านการเดินทางมา" ไม่ใช่ "ถูกขยำ" |
| `MAX_ANIM` | 6 ฉบับ | เกินนั้นวาดเป็นจุดนิ่ง ไม่ animate พายุ |

### โลก — ยกเครื่องรอบ 22 ส.ค. 69

โลกเดิมเป็นทรงกลมใสมีเส้นกริด ไม่มีข้อมูลอะไรเลย จดหมายบินบนความว่างเปล่า → เป็นของประดับ ไม่ใช่สนามที่เรื่องเกิดขึ้น

- **เส้นชายฝั่งจริง** — Natural Earth 110m (public domain) → Douglas-Peucker 0.35° → 101 เส้น / 2,105 จุด / **8.5KB** ฝังในไฟล์เดียว
  ตรวจความถูกต้องด้วยระยะจากเมืองจริงถึงชายฝั่งที่ใกล้สุด: กรุงเทพ 58 กม. · โตเกียว 63 · เคปทาวน์ 22 · เรคยาวิก 43 · ซิดนีย์ 227 — ส่วนเมืองในแผ่นดิน 1,086–1,311 กม.
- **กลางวัน-กลางคืนจากดวงอาทิตย์จริง** — subsolar point จากเวลา UTC + วันในปี (ตรวจแล้ว: 06:00 UTC → ลองจิจูด 90° · ส.ค. → +11.5° · ธ.ค. → −23.4°)
- **กลางคืนเป็น "ไม่มีแสง" จริง ไม่ใช่แค่จางลง** — คำนวณรูปทรงของซีกมืดจากเส้นแบ่งจริง แล้วเติมพื้นมืด · เส้นชายฝั่งฝั่งมืดกลับเป็นสีอ่อน · ไฟเมือง 202 เมือง (Natural Earth, ~1KB) ติดเฉพาะฝั่งที่มืดแล้ว · เส้นขอบแสง + ชั้นบรรยากาศทอง/น้ำเงินที่ขอบโลก
  วัดจริง: กลางวัน 174–190 · กลางคืน 71–93 (พื้นกระดาษ 231) — จากเดิมที่ต่างกันแค่ 220 vs 199
- **ซีกหลังเห็นจาง ๆ** (×0.13) แทนที่จะหายสนิท — แก้สเปกเดิมโดยตั้งใจ แต่เส้นทางจดหมายยังตัดสนิท
- **perf** — โลกเป็น layer แยก วาดใหม่เฉพาะตอนหมุน: ทั้งฉาก 2.84ms/เฟรม (ตอนหมุน 3.45ms) จากงบ 16ms
- ไฟล์โตจาก 72KB → 88KB

### งานภาพที่จูนแล้วในรอบนี้ (art open ของ ART SPEC §7)

- **องศาเอียงฝน** — เดิมเอียงเต็ม 30° ตลอด แก้เป็นแปรตามความแรงลมจริง (`dir = boost / WIND_CAP`) → ลมเบาฝนเกือบตรง ลมแรงเอียงสุด ~30°
- **สี/contrast globe ตอนหลายเส้น** — เส้นทางไล่ 3 ระดับ: ที่เลือก .92 · ที่ยังบิน .48 · ที่ถึง/หายแล้ว .28
- **เพดานร่องรอย** — อายุอิ่มตัวที่ 7 วัน (เดิม 6) และสีเก่าสุดอ่อนลง `[216,203,171]` → ยังอ่านว่า "ผ่านการเดินทาง" ไม่ใช่ "เสียหาย"
- **พายุบน globe** — ตัด wisps ออก (rich=false) ตามกฎ LOD; รายละเอียดเต็มเก็บไว้หน้าพยากรณ์
- **เส้นใยกระดาษ** — เดิมระยะเท่ากันจนดูเป็นเส้นบรรทัดสมุด แก้เป็นสุ่มไม่สม่ำเสมอ + จางลงครึ่งหนึ่ง

**ยังต้องดูด้วยตาบนมือถือจริง:** ความเข้มรวมของร่องรอยตอน `days≥5`, ความหนาแน่นพายุตอนมีจดหมาย 5+ เส้นพร้อมกัน

---

## กฎที่บังคับไว้ในโค้ด

- ร่องรอยฝน **ถูกดันไปขอบกระดาษเสมอ** (`rainBlooms` บีบ u เข้าขอบ) — ห้ามทับตัวอักษร
- พายุบน globe วาดเฉพาะจุดที่ z≥0 และ **ไม่ใส่ wisps** (รายละเอียดเต็มเก็บไว้ตอนดูใกล้)
- เปิด sheet ใด ๆ = globe หยุดวาด (LOD: ของหนักกับของเยอะไม่ทำงานพร้อมกัน)
- `document.hidden` = หยุด animation · `prefers-reduced-motion` = เดินเวลาช้าลง 4 เท่า
- DPR cap 2 ทุก canvas
- localStorage ใช้ไม่ได้ (private mode ฯลฯ) → fallback ในหน่วยความจำ + เตือนผู้ใช้ ไม่พังทั้งแอป

---

## ข้อจำกัด — สถานะหลังรอบเคลียร์

### แก้/บรรเทาแล้ว

**1. ลิงก์ผูกกับที่อยู่ไฟล์** → เหลือความเสี่ยงเฉพาะกรณีที่ยังไม่ได้ host
- เพิ่ม **ไฟล์จดหมาย (.json)** ส่งแทนลิงก์ได้ — ผู้รับกด Import
- เพิ่ม **รับจดหมายจากรหัส** — วางลิงก์ทั้งอันหรือเฉพาะรหัสก็ได้ ตัดช่องว่าง/ขึ้นบรรทัดให้เอง (กันแชทตัดลิงก์)
- ถ้าเปิดแอปจาก `file://` **แอปเตือนตอนกดส่งทันที** ว่าลิงก์นี้คนอื่นเปิดไม่ได้ ให้ส่งเป็นไฟล์แทน
- อุดช่องโหว่ที่เจอระหว่างทำ: ไฟล์จดหมายฉบับเดียวไม่มีฟิลด์ `dir` → ถ้าไม่เดาให้ถูกตอน import **ผู้รับจะอ่านได้ก่อนจดหมายถึง** (ตอนนี้ตัดสินจากชื่อผู้ส่งแล้ว)

**2. ผู้รับไม่รู้ว่ามีจดหมายกำลังมา** → บรรเทาได้มากโดยไม่ต้องมี backend
- **ไฟล์ปฏิทิน .ics** — กดครั้งเดียวตอนรับจดหมาย ปฏิทินในเครื่องจะเตือนเองตอนถึง **ไม่ต้องเปิดแอปค้าง** (RFC 5545 ครบ: escape + fold บรรทัด ไทยยาวเกิน 75 ไบต์)
- **Notification ของเบราว์เซอร์** — ถ้าเปิดแอปค้างไว้ เตือนตอนถึงจริง
- **เช็คย้อนหลังตอนเปิดแอป** — ถึง/หายระหว่างที่ไม่อยู่ ได้รู้ทันทีที่เปิด

### ยังอยู่ (จงใจ หรือแก้ไม่ได้ในเฟส 1)

3. **พยากรณ์ = ณ วันส่ง** ไม่ใช่อากาศจริงที่เกิดจริง — ตั้งใจ (จดหมายสมัยก่อนก็ออกไปพร้อมความไม่แน่นอน)
4. **"อ้อม" ไม่ใช่เส้นเลี่ยงจริง** — เป็น +เวลา / −โอกาสหาย ตามที่ออกแบบ (simplicity > สมจริง)
5. **หมอก** — เก็บไว้เฟสหลังตามที่ตัดสินไว้
6. **ผู้รับต้องเปิดลิงก์/รับจดหมาย 1 ครั้งก่อน** ถึงจะตั้งเตือนได้ — ถ้าไม่เคยเปิดเลย ไม่มีอะไรไปบอกเขาได้ **นี่คือข้อเดียวที่ต้องรอ backend จริง ๆ**
7. **ผู้ส่งไม่รู้ว่าผู้รับเปิดอ่านหรือยัง** — ฝั่งผู้ส่งเห็นแค่ "ถึงปลายทางแล้ว" ตามเวลา (จะรู้ต้องมี backend)
8. **นาฬิกาเครื่อง** — เวลาถึงคำนวณจากนาฬิกาของแต่ละเครื่อง ถ้าเครื่องใดตั้งเวลาเพี้ยน ตัวเลขจะเพี้ยนตาม
9. **บนมือถือผ่าน http ธรรมดา (LAN)** — ปุ่ม GPS และ service worker ไม่ทำงาน เพราะเบราว์เซอร์บังคับ https (ที่เหลือใช้ได้ครบ; ค้นชื่อเมืองแทน GPS ได้)

---

## ทดสอบแล้ว

**กลไก** — geocoding ไทย/อังกฤษ · Open-Meteo 13 จุด/คำขอเดียว · จำลองเวลาทั้งเส้น ·
floor 6 ชม. ที่ระยะ 15 กม. · เส้นไกลสุดที่ทดสอบ ฉะเชิงเทรา→อูซัวยา 15,351 กม. = 1 วัน 15 ชม. (ตรงสเกล)

**การส่ง-รับจริงบน URL จริง** (`http://localhost:8777`) — ส่ง → ได้ลิงก์ 594–646 ตัวอักษร →
ล้าง localStorage (จำลองเป็นคนละเครื่อง) → เปิดลิงก์ → จดหมายเข้าคลังเป็นขาเข้า →
**อ่านไม่ได้จนกว่าจะถึง** (เห็นสถานะ/ETA แทน) → ครบวง

**สถานะทั้ง 4** — กำลังบิน / จอดรอพายุ / ถึงแล้ว / หายกลางพายุ ถูกต้องทั้ง chip และหน้าอ่าน

**มือถือ (จำลอง 375×812)** — ไม่มี horizontal overflow ทุกหน้า · ปุ่ม/ช่องกรอกทุกตัว ≥44px ·
ช่องกรอก 16px (กัน iOS ซูมตอนโฟกัส) · service worker ลงและแคช shell สำเร็จ

**ทางส่งสำรอง** — วางรหัสที่มีช่องว่าง/ขึ้นบรรทัดปน → รับได้ · วางลิงก์ซ้ำ → ไม่เพิ่มซ้ำ ·
รหัสผิดรูป → เตือนแล้วไม่พัง · ไฟล์จดหมายฉบับเดียว import แล้วเป็นขาเข้า **อ่านไม่ได้จนกว่าจะถึง** ·
.ics ไม่มีบรรทัดเกิน 75 ไบต์ และ escape ชื่อที่มีจุลภาคถูกต้อง

**ยังไม่ได้ทดสอบบนมือถือจริง** ← ต้องทำก่อนใช้จริงตามกฎข้อ 5 (จำลองแทนไม่ได้เรื่อง touch/ความเร็วเครื่อง)

---

## ที่ยังค้างจริง ๆ

- **ชื่อแอป** — "จดหมายสายลม" ยังเป็น working title (ต้องตัดสินเอง)
- **เฟส 2 (backend)** — ยังไม่ออกแบบตามที่ตกลง: user system / อายุจดหมาย / แจ้งเตือน / PDPA

---

## วิธี build `drift-demo.html` (อัปเดต 23 ส.ค. 2569 หลังพอร์ต)

หน้าโชว์ใช้โค้ดแกนชุดเดียวกับ `index.html` โดย**ตัดออกมา ไม่ใช่ก๊อบมาแก้**
ช่วงที่ตัดคือตั้งแต่บรรทัดถัดจาก `<script>` แรก จนจบ `drawPaper()` — อย่าใช้เลขบรรทัดตายตัว
เพราะขยับทุกครั้งที่แก้ `index.html` ให้หาปลายทางด้วย `grep` แทน:

```bash
SP=<scratchpad>
E=$(grep -n "ctx.strokeRect(.5,.5,W-1,H-1);" index.html | cut -d: -f1); E=$((E+1))
sed -n "221,${E}p" index.html > "$SP/core.js"
cat "$SP/demo_shell.html" "$SP/core.js" "$SP/drift_sim.js" "$SP/demo_app.js" > drift-demo.html
printf '\n</script>\n</body>\n</html>\n' >> drift-demo.html
```

`demo_app.js` **ต้องไม่เขียนทับฟังก์ชันของแกน** — ถ้าพบว่าต้องเขียนทับ แปลว่าแกนควรแก้
ตอนนี้เหลือแค่การตั้งค่า: `SUN_MS` (ป้อนเวลาที่ย่อแล้ว) และ `SUN_BUCKET=9e5` (ลดความถี่วาดโลกใหม่)

ตรวจหลัง build: เปิดหน้า → ไม่มี error ใน console → `data` ไม่เป็น null (ดึงลมจริงสำเร็จ) →
วัดวงเปิดกลางคืนในวง/นอกวงควรได้ราว 155/181

## build หน้าโชว์ทั้งสองหน้า (อัปเดต 23 ส.ค. 2569)

ตอนนี้มีสองหน้า สร้างจากชิ้นส่วนเดียวกัน ต่างกันแค่ต้นทาง-ปลายทาง:
`drift-demo.html` (กรุงเทพ→แวนคูเวอร์) และ `drift-th.html` (ฉะเชิงเทรา→เชียงใหม่)

```bash
python <scratchpad>/build_demos.py
```

สคริปต์ตัดแกนจาก `index.html` ใหม่ทุกครั้ง (หาปลายด้วย `grep` ไม่ใช้เลขบรรทัด)
แล้วสร้างทั้งสองหน้า — **แก้ที่เดียวได้ทั้งคู่ อย่าแก้ไฟล์ .html ตรง ๆ**

`drift_sim.js` ต้องใช้ `driftStep()` และกฎเวลาฐานของแกนเสมอ
ถ้าหน้าโชว์คำนวณเองจะบอกเวลาคนละชุดกับแอปจริง (เคยเจอ: 1.5 ชม. เทียบกับ 5.8 ชม.)

**อย่าใช้เลขบรรทัดหาแกน** — `build_demos.py` เคยใช้ `idx[220:...]` แล้วพังทันทีที่เพิ่ม markup
ในหน้า HTML เพราะจุดเริ่มของ `<script>` เลื่อนลง ผลคือ HTML หลุดเข้าไปในไฟล์ JS
ตอนนี้หาหัวด้วยเครื่องหมาย `no-backend` และหาปลายด้วย `ctx.strokeRect(...)`

## แคชลมของภูมิภาค (เพิ่ม 24 ส.ค. 2569 · ข้อ 58)

**ตั้งแต่นี้ repo มีขั้นตอน build ข้อมูล ไม่ใช่แค่ build โค้ด**

```bash
python tools/fetch_wind.py
```

ดึงลมทั้งภูมิภาค 1,480 จุด แล้วเขียน `wind/th.bin` (55 KB) กับ `wind/index.json`
ใช้ไลบรารีมาตรฐานล้วน ไม่ต้อง `pip install` อะไร · ใช้เวลาราว 2 นาที (มี `time.sleep` กันชนเพดาน 600 ครั้ง/นาที)

**บนของจริงไม่ต้องรันมือ** — `.github/workflows/wind.yml` รันให้ทุก 6 ชั่วโมงแล้ว commit ไฟล์กลับเข้า repo
กดรันเองได้ที่แท็บ Actions (`workflow_dispatch`) ตอน cron หลุดหรือตอนทดสอบ

**ถ้าจะเพิ่มพื้นที่** แก้ `TILES` ในสคริปต์อย่างเดียว แอปอ่านจาก `index.json` และเลือกแผ่นเอง
แต่**ต้องคิดโควตาก่อน**: 1 จุด = 1 คำขอ เพดานฟรี 10,000/วัน
สูตร: `จำนวนจุดรวม × (24 ÷ ชั่วโมงต่อรอบ) ≤ 10,000` — ตอนนี้ 1,480 × 4 = 5,920

**อย่าลดรอบให้ถี่กว่า 6 ชม.** ต้นทางอัปเดตทุก 3-6 ชม. และหนึ่งคำขอได้พยากรณ์ล่วงหน้า 96 ชม.อยู่แล้ว
ดึงถี่กว่านั้นเผาโควตาเพื่อข้อมูลชุดเดิม

**ตรวจหลังรัน:** เปิดแอป → console ไม่มี error → ใน devtools พิมพ์ `WX_SRC` หลังส่งจดหมายในไทย
ต้องได้ `cache:th` · ถ้าได้ `api` แปลว่าแคชหมดอายุหรือกล่องไม่ครอบ (ทั้งสองกรณีแอปยังทำงานได้ แค่เปลืองโควตา)

**ยังไม่มี git repo** — ต้อง `git init` + ต่อ GitHub remote ก่อน workflow ถึงจะรันได้จริง
ระหว่างนี้รันสคริปต์เองแล้วอัปไฟล์ `wind/` ขึ้น host ได้ผลเหมือนกัน

## ต่อ repo เข้า GitHub (24 ส.ค. 2569)

`git init` เรียบร้อยแล้ว มี commit แรกครบ 22 ไฟล์ · ตั้งชื่อ/อีเมลไว้ **เฉพาะ repo นี้** ไม่ได้แตะค่า global

**ไม่เข้า repo (ยังอยู่ในเครื่องเหมือนเดิม):** `index.backup-*.html` · `files.zip` · `.claude/`

**`.gitattributes` มีสองหน้าที่ อย่าลบ**
1. บังคับไฟล์ข้อความเป็น LF — workflow รันบน Linux ถ้าบล็อก `run: |` เป็น CRLF จะพังที่ `if/then`
2. `*.bin binary` — แคชลมเป็นไบต์ดิบบีบ zlib ถ้า git แปลงบรรทัดให้ ไฟล์จะเสียเงียบ ๆ
   แล้วไปโผล่เป็น "ลมเพี้ยน" ซึ่งตามต้นตอยากมาก (ตรวจแล้ว: ไฟล์ในบล็อบตรงกับในเครื่องทุกไบต์)

### ขั้นตอนที่เหลือ (ต้องทำจากเครื่องที่ล็อกอิน GitHub อยู่)

```bash
git remote add origin https://github.com/<ชื่อผู้ใช้>/drift.git
git push -u origin main
```

### ⚠️ ตั้งค่า Pages ให้ถูก ไม่งั้นลมจะไม่อัปเดต

Settings → Pages → Source ต้องเลือก **"Deploy from a branch"** (branch `main`, folder `/`)

**ห้ามเลือก "GitHub Actions"** เป็น source — เพราะ commit ที่ push ด้วย `GITHUB_TOKEN`
**ไม่ trigger workflow อื่น** ตามกฎของ GitHub เอง ผลคือแคชลมจะถูก commit แต่ไม่เคยถูก deploy
หน้าเว็บจะค้างอยู่กับลมของรอบแรกตลอดไปโดยไม่มีอะไรฟ้อง

### เรื่องที่ต้องรู้เกี่ยวกับ Actions

- cron **ช้าได้ 5-30 นาที** เป็นเรื่องปกติของ GitHub ไม่ใช่ของเสีย
- GitHub **ปิด scheduled workflow อัตโนมัติเมื่อ repo เงียบเกิน 60 วัน**
  [ไม่แน่ใจ] ว่า commit ที่มาจากบอทนับเป็น "ความเคลื่อนไหว" หรือไม่
  GitHub จะส่งอีเมลเตือนก่อนปิด — ถ้าโดนปิด กดเปิดใหม่ที่แท็บ Actions ได้
- กดรันเองได้ตลอดที่แท็บ Actions → drift wind cache → Run workflow (`workflow_dispatch`)

### repo สาธารณะหรือส่วนตัว

- **สาธารณะ** — Pages ใช้ได้ฟรี แต่โค้ดและเอกสารทั้งหมดเปิดให้คนอ่าน
- **ส่วนตัว** — Actions ยังทำงาน แต่ Pages ต้องมี GitHub Pro

ไม่มีข้อมูลส่วนบุคคลอยู่ใน repo (จดหมายทุกฉบับอยู่ใน localStorage ของแต่ละเครื่อง ไม่เคยขึ้น repo)
